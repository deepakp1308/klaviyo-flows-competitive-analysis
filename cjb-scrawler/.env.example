# Copy to cjb-scrawler/.env (gitignored). Never commit real values.
MC_EMAIL=your-mailchimp-login@example.com
MC_PASSWORD=your-app-password-or-login-password
# Optional label for HTML report only (not sent to Mailchimp)
MC_ACCOUNT_LABEL=Scrawler QA account
